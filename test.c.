#include "libft.h"
#include <stdio.h>
#include <string.h>

int	main(void)
{
	char a[] = "Hello World";
	ft_memset(a, 1000, 5);
	printf("%s\n", a);
	char b[] = "Hello World";
	memset(b, 1000, 5);
	printf("%s\n", b);
}
